"A post created by a user"
type Query {
  posts: [Posts!]!
}

type Posts {
  id: ID!
  title: String!
  content: String!
  thumbnail: String
  author: Author!
  createdAt: String!
}

"An author of posts"
type Author {
  id: ID!
  name: String!
  photo: String
}
